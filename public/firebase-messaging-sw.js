// public/firebase-messaging-sw.js
// Optional: background notifications (web).
// If you plan to use FCM on the web, you will need to configure this service worker
// and use the 'compat' libraries OR the new modular SW. This is a placeholder.
self.addEventListener('push', function(event) {
  // Basic fallback handler so the SW file is valid and doesn't 404.
});
